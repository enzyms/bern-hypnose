---
import BaseLayout from '../layouts/BaseLayout.astro';
import ArrowLeft from '../icons/ArrowLeft.astro';
---

<BaseLayout description="Gutschein">
    <a class="flex items-center absolute text-red-500 hover:text-red-700 -mt-8 text-sm font-bold" href="/">
        <ArrowLeft class="w-4 h-4 fill-current" />
        <span>Home</span>
    </a>
    <h1 class="mb-8">Gutschein</h1>

    <p class="text-lg text-gray-700 mb-8">
        Möchtest du jemandem eine Freude machen? Mit einem Gutschein kannst du eine Hypnosetherapie-Sitzung im Wert von <strong>150 CHF</strong> verschenken. Nach
        dem Ausfüllen des Formulars wirst du eingeladen, den Betrag per Twint zu bezahlen. Anschließend sende ich dir den Gutschein per E-Mail.
    </p>

    <form name="gutschein" method="POST" netlify class="max-w-lg mx-auto bg-red-100 shadow-md rounded-lg p-6 space-y-4">
        <!-- Hidden input for Netlify -->
        <input type="hidden" name="form-name" value="gutschein" />

        <h2 class="text-2xl font-semibold text-gray-800 text-center">Gutschein verschenken</h2>

        <div class="space-y-2">
            <label for="first-name" class="block text-sm font-medium text-gray-700">Dein Vor- und Nachname</label>
            <input
                type="text"
                id="first-name"
                name="first-name"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-red-500 focus:border-red-500 text-gray-700"
                required
            />
        </div>

        <div class="space-y-2">
            <label for="email" class="block text-sm font-medium text-gray-700">Deine E-Mail-Adresse</label>
            <input
                type="email"
                id="email"
                name="email"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-red-500 focus:border-red-500 text-gray-700"
                required
            />
        </div>

        <div class="space-y-2">
            <label for="recipient-name" class="block text-sm font-medium text-gray-700"> Vor- und Nachname der beschenkten Person </label>
            <input
                type="text"
                id="recipient-name"
                name="recipient-name"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-red-500 focus:border-red-500 text-gray-700"
                required
            />
        </div>

        <div>
            <button
                type="submit"
                name="subscribe"
                class="inline-flex items-center justify-center w-full px-6 py-3 text-base font-bold text-white bg-red-600 border border-red-600 rounded-full transition hover:bg-red-500 hover:text-red-50"
            >
                Gutschein anfordern
            </button>
        </div>
    </form>
</BaseLayout>
